#include <iostream>

int main()
{
  /* This is a comment that extends over several lines
  because /* and */ as its starting and ending delimiters */
  std::cout << "Does this Work?" << std::endl;

  return 0;
}
// Doesn't work because the inner comments interferea
